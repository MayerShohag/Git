git setup & configuration:
git version check : git --version

configuration:
git config >> usage: git config [<options>]

set global username:
git config --global user.name "set your username"

set different repository username: 
git config user.name "set your username"

set email:
git config --global user.email "set your email address"

confirm username or email will be set:
git config --list

confirm which username will be set:
git config user.name

confirm which useremail will be set:
git config user.email
